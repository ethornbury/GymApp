class StaticPageController < ApplicationController
  def home
  end

  def contact
  end
  
  #added this after generating the controller
  #need to also create the relevant view about.html.erb in the views>static_page
  
  def about
  end
end
